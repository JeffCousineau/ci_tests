version: v1.0
name: Pytest tests
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Python example
    task:
      jobs:
        - name: Pytest
          commands:
            - pytest -v
      prologue:
        commands:
          - pip install -U pytest
